# please-humanize

한국어 업무 메시지와 기술 문서를 자연스럽고 읽기 쉽게 다듬는 AI 스킬입니다.

번역투로 늘어진 문장을 구체적인 행동으로 풀어 쓰고, 이해 순서대로 정보를 다시 배치합니다. 원문의 사실, 날짜, 조건, 시제와 불확실성은 그대로 둡니다.

## 언제 쓰나요

새로 쓸 때가 아니라 이미 있는 글이 어색할 때 쓰는 스킬입니다.

- 뜻은 맞는데 읽기 어렵습니다. "정합성 확보를 위한 검증 수행"처럼 동작이 명사 속에 숨은 문장이 이어집니다.
- AI가 쓴 초안입니다. 문장은 매끄러운데 같은 결론을 세 번 바꿔 말하거나, 원문에 없던 이유와 효과가 들어가 있습니다.
- 개발 업데이트나 릴리즈 노트인데 변경 내용만 다 적혀 있고, 무엇을 먼저 봐야 할지 모르겠습니다.
- 슬랙 메시지나 업무 메일에서 정중하게 쓰려다 요청이 흐려졌습니다. 거절이나 일정 경계가 뭉개졌습니다.
- 쉼표를 어미마다 넣었는지, 날짜와 띄어쓰기 표기가 맞는지 함께 보고 싶습니다.

## 사용 예시

아래는 설명을 위해 만든 예시입니다. 결과는 사용하는 모델과 문맥에 따라 달라질 수 있습니다.

**요청**

> please-humanize로 아래 릴리즈 노트를 다듬어줘. 조건과 시제는 유지해줘.

**다듬기 전**

> 본 릴리즈에서는 파이프라인 실행 완료 후 잔존하는 중간 산출물에 대한 자동 정리 기능이 추가 반영되었습니다. 이를 통해 스토리지 사용량 최적화가 가능해지며, 운영 효율성 제고에 기여할 것으로 기대됩니다. 실행 성공 시 즉시 삭제 처리가 수행되므로, 실행 종료 후에도 파일이 누적되는 이슈는 해소되었습니다. 다만 단일 노드 재실행 불가 제약이 존재하는 점에 대한 주의가 필요합니다.

**다듬은 후**

> 이번 버전부터는 파이프라인 실행에 성공하면 중간 파일을 바로 삭제하여 실행 후에도 스토리지에 파일이 쌓이는 문제를 해결했습니다.
>
> 다만 중간 파일을 삭제한 뒤에는 앞 노드의 결과를 쓰는 노드만 따로 다시 돌릴 수 없습니다. 이때는 파이프라인 전체를 다시 실행해야 합니다.

무엇이 달라졌는지 보면 이렇습니다.

- `운영 효율성 제고에 기여할 것으로 기대됩니다`는 통째로 사라졌습니다. 원문에 수치도 관찰 결과도 없어서 남길 근거가 없습니다.
- 같은 결론을 세 번 바꿔 말한 것을 한 문장으로 합쳤습니다. `자동 정리 기능이 추가 반영` · `스토리지 사용량 최적화` · `파일이 누적되는 이슈는 해소`가 모두 같은 얘기입니다.
- `단일 노드 재실행 불가 제약이 존재하는 점에 대한 주의가 필요합니다`는 무엇을 조심할지 알려주지 않습니다. 어떤 노드가 왜 안 되고 그때 무엇을 해야 하는지로 풀었습니다.
- `수행되므로,` 뒤의 쉼표를 뺐고, `잔존하는 중간 산출물`은 `중간 파일`로 불렀습니다.

## 다듬는 기준

1. 핵심 상태나 변경 사항을 먼저 설명합니다.
2. 독자가 이해할 순서로 정보를 배치합니다.
3. 명사 속에 숨은 동작을 목적어와 동사로 풀어 씁니다.
4. 이유·조건·결과를 자연스럽게 연결하고 필요한 곳에만 쉼표를 씁니다.
5. 글의 종류와 상대에 맞게 격식을 조절합니다.

## 하지 않는 것

- 원문에 없는 이유, 수치, 일정, 담당자를 만들지 않습니다.
- 아직 확인 중인 일을 다 끝난 일로, 그럴 것 같다는 예상을 약속으로 바꾸지 않습니다. 반대로 이미 실패한다고 확인된 일을 "실패할 수 있습니다"처럼 가능성으로 바꾸지도 않습니다.
- "인증", "배포", "동기화"처럼 정확하고 익숙한 기술 용어를 쉬운 말로 바꾸지 않습니다. 코드 식별자, 버전, 파일명, ISO 날짜 표기도 그대로 둡니다.
- 메시지를 대신 보내지 않습니다. 바로 보낼 수 있게 글만 다듬어 드립니다.
- 문체를 다듬을 뿐 구현이나 사실 관계를 검증하지는 않습니다.
- 규범에서 허용하는 여러 표현 중 하나를 선호할 뿐, 다른 표현을 틀렸다고 단정하지 않습니다.

## 설치

Claude Code의 스킬 디렉터리에 내려받으면 됩니다. 혼자 모든 프로젝트에서 쓸 때는 개인 디렉터리에 둡니다.

```bash
git clone https://github.com/ddoriddori/please-humanize.git ~/.claude/skills/please-humanize
```

한 프로젝트에서 팀과 함께 쓸 때는 프로젝트 안에 두고 커밋합니다.

```bash
git clone https://github.com/ddoriddori/please-humanize.git .claude/skills/please-humanize
```

`SKILL.md`와 `references/` 디렉터리가 `please-humanize` 디렉터리 안에 그대로 있어야 스킬을 찾습니다. Claude Code를 다시 시작한 뒤 아래 사용 예시처럼 스킬 이름을 부르면 적용됩니다. 나중에 최신 내용을 받을 때는 내려받은 디렉터리에서 `git pull`을 실행하세요.

붙여 넣는 원문은 그대로 모델에 전달되고, 스킬은 코드 식별자와 버전 표기를 보존합니다. 로그나 설정에 섞인 토큰, 키, 고객 식별 정보는 붙여 넣기 전에 지우세요.

## 어떻게 쓰나요

스킬 이름과 다듬을 원문을 함께 보내면 됩니다. 여기에 지켜야 할 것을 한 줄 적으면 결과가 더 안정적입니다. 원문의 사실과 조건은 스킬이 보존하지만, 구조를 어디까지 바꿀지와 분량을 얼마나 줄일지는 글마다 판단이 갈립니다.

| 적을 것 | 예 |
| --- | --- |
| 글의 종류와 독자 | PM과 배포 담당자가 같이 보는 릴리즈 노트야 |
| 유지할 것 | 목차와 티켓 번호, 표는 그대로 둬 |
| 격식 | 외부 고객한테 보내는 메일이야 / 친한 동료한테 보내는 슬랙이야 |
| 범위 | 문서 전체 / 03절만 |

그대로 복사해 쓸 수 있는 요청입니다.

- `please-humanize로 이 릴리즈 노트 다듬어줘. 버전과 조건은 그대로 두고 순서만 읽기 쉽게 바꿔줘.`
- `please-humanize로 이 슬랙 메시지 다듬어줘. 정중하게 쓰되 이번 주엔 어렵다는 건 분명히 남겨줘.`
- `please-humanize 기준으로 쉼표와 표기만 봐줘. 문장 구조는 건드리지 말고.`

스킬은 글의 종류를 보고 참고 문서를 골라 읽습니다. 기술 문서와 릴리즈 노트에는 `references/technical-writing.md`의 순서를, 표기·쉼표·번역투 교정에는 `references/korean-style.md`의 기준을 적용합니다. 메일과 슬랙에는 `SKILL.md`의 메시지 처리 순서를 씁니다.

## 파일 구성

| 경로 | 내용 |
| --- | --- |
| SKILL.md | 스킬의 기본 지침과 글 종류별 처리 순서 |
| references/technical-writing.md | 기술 문서의 구성, 설명 방식과 편집 예시 |
| references/korean-style.md | 한국어 표현, 표기와 쉼표 사용 기준 |
| README.md | 소개와 사용 방법 |
| LICENSE | MIT 라이선스 |

## 원본과 외부 자료

업무에서 메시지와 기술 문서를 고치며 정리한 편집 선호를 스킬로 옮겼습니다. 스킬 본문과 참고 문서는 기존 원본을 그대로 포함했고, 선택적인 화면 표시 설정과 아이콘은 넣지 않았습니다.

국립국어원의 규범·해설과 개발 블로그를 참고한 출처는 참고 문서 안에 있습니다. 외부 자료는 해당 권리자의 권리를 따르며, 이 프로젝트의 라이선스는 링크된 외부 자료에 적용되지 않습니다. 예시는 공식 규정의 예문이나 실제로 고객에게 보낸 글이 아닙니다.

## 라이선스

MIT License. 저작권 고지와 라이선스를 유지하는 조건으로 사용·수정·재배포와 상업적 이용을 허용합니다. 자세한 조건은 [LICENSE](LICENSE)를 확인하세요.
